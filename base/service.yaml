apiVersion: v1
kind: Service
metadata:
  name: spring-petclinic-app-service
spec:
  selector:
    app: spring-petclinic-app 
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
